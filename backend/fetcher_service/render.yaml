services:
  - type: web
    name: time-to-sell-fetcher
    env: python
    rootDir: backend/fetcher_service
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
